const getters = {
    // cms
    userInfo: state => state.common.userInfo,
    imageUrl: state => state.common.imageUrl,
    baiduAk: state => state.common.baiduAk,
    token: state => state.common.token,
    getTabbarShow: state => state.common.tabbarShow,
	
    
}
export default getters
  